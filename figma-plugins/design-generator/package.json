{
  "name": "craftgarden-design-generator",
  "version": "1.0.0",
  "description": "Figma plugin that generates the craftgarden.studio design system and page layout",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
